#include<stdio.h>
#include<string.h>
int main()
{
	char a[100];
	int i,n,b=0;
	printf("enter");
	gets(a);
	n=strlen(a);
	for(i=0;i<n/2;i++)
	{
		if(a[i]==a[n-i-1])
			b++;	
	}
	if(b==i)
	printf("palindrome");
		else
			printf("no");	
}
