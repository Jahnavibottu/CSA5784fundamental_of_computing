#include<stdio.h>
long fact(int n)
{
	if(n==0)
	return 1;
	else 
	return n*fact(n-1);	
}
int main()
{
	int num,factorial;
	printf("enter");
	scanf("%d",&num);
	factorial=fact(num);
	printf("factorial=%d",factorial);
}
