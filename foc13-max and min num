#include<stdio.h>
int main()
{
	int i,a[10],n,s,l;
	printf("enter size");
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	l=a[0];
	s=a[0];
	for(i=0;i<n;i++)
	{
		if(a[i]>l)
		l=a[i];
		else if(a[i]<s)
		s=a[i];
	}
	printf("large %d",l);
	printf("small %d",s);
}
