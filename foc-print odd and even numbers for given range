#include<stdio.h>
int main()
{
	int a,i;
	printf("enter the number:");
	scanf("%d",&a);
	printf("even numbers are:");
	for(i=2;i<=a;i++)
	{
		if(i%2==0)
		{
			printf("%d,",i);
		}
	}
	printf("odd numbers are:");
	for(i=2;i<=a;i++)
	{
		if(i%2!=0)
		{
			printf("%d,",i);
		}	
}
}
