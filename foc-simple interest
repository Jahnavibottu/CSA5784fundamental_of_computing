#include<stdio.h>
int main()
{
	int p,t,r;
	float si;
	printf("principle:");
	scanf("%d",&p);
	printf("time:");
	scanf("%d",&t);
	printf("rate:");
	scanf("%d",&r);
	si=p*t*r/100;
	printf("si:%f",si);
	
}
